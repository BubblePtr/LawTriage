# 律所 AI 外呼分诊系统 — 演示版（Pre-MVP）PRD

| | |
|---|---|
| **版本** | v0.1 · 2026-05-29 |
| **阶段** | 演示阶段（Pre-MVP，客户复演用） |
| **关键约束** | 输入通道 = RTC（浏览器实时音频）；语音 Agent = 级联（ASR → LLM → TTS） |
| **承接文档** | 《律所 AI 外呼分诊系统 — MVP 技术设计 v2》 |

---

## 1. 背景与目标

### 1.1 背景

生产版主链路是 **PSTN 电话外呼**，但运营商号码采购与公安备案有不确定的 lead time。为在备案完成前向客户演示完整的"分诊接待"体验、确认产品方向、推动立项，本阶段先做一个**演示版**：

- 用 **RTC（浏览器实时音频）作为输入通道**替代真实电话——客户在演示现场用浏览器/手机即可体验，不依赖号码与备案。
- 语音 Agent 走**级联方案**，与生产版完全一致，便于演示资产直接复用。
- RTC 与 PSTN 在架构上通过 **MediaSession 抽象层**对接：演示版的 Agent 代码在生产期**零改动**切换到 SIP/电话，RTC 只是"换了个进音口"。

### 1.2 演示目标（要向客户证明的四件事）

1. **像真人**：AI 能完成一通听起来自然、不卡顿、可被打断、问得专业的接待对话——直击客户"一听就不是真人 / 问卡了"的头号担忧。
2. **会分诊**：通话中能把分诊要素问全，挂断后自动产出**结构化客户档案 + 案件大/中/小分级 + 预约信息**。
3. **能过滤**：敏感 / 无效 / 恶意咨询会被识别并标记，不污染律师视图。
4. **看得见**：客户在演示后台**亲眼看到**上述结构化数据与录音回放，建立"这套东西能落地"的信心。

### 1.3 非目标（本阶段明确不做）

PSTN 电话外呼、运营商号码 / 公安备案 / 号码池轮呼、多渠道引流（抖音/小红书/美团等）、企业微信承接、不挂断转人工、坐席系统、诉讼费精算 / 管辖法院判断、多细分领域知识库、声音克隆（列为可选拉伸项，见 §4 F3）。以上均属生产版，不进演示。

---

## 2. 范围与关键技术决策

| 决策项 | 演示版选择 | 理由 | 生产版对照 |
|---|---|---|---|
| 输入通道 | **RTC 浏览器实时音频** | 绕开号码采购/备案，现场即可演 | 切 SIP/PSTN，经 MediaSession 抽象层，Agent 不改 |
| 语音 Agent | **级联**：流式 ASR → 文本 LLM（话术+RAG+FC+结构化输出）→ TTS | 可控、可审计、能插知识库与结构化产出，演示资产可复用 | 完全一致；端到端 S2S 留作后续 A-B |
| 对话框架 | LiveKit Agents 或 Pipecat（拿现成 VAD / 端点检测 / 打断） | 演示要"不卡、能打断"，框架最省事 | 同栈延用 |
| 知识库 | **单一细分领域**（建议婚姻家事，待律师定） | 演示聚焦、问答准确率高 | 扩展多领域 |
| 入口 | 演示用留资页（手机号 + 案件类型）→ "开始咨询"进 RTC | 既展示留资动作，又即时接通 | 各渠道引流位 → 留资 → 电话外呼 |

---

## 3. 演示场景与角色

### 3.1 角色

- **当事人**（演示者扮演）：在浏览器/手机端发起咨询、口述案情。
- **观众**（客户方律所负责人）：观看大屏后台，关注体验与结构化产出。
- **AI 分诊 Agent**：用资深律师话术接待、收集要素、给出预约。

### 3.2 主线场景（示例：婚姻家事 — 离婚财产分割）

当事人进入 → AI 开场自我介绍并说明"先帮您做初步登记" → 引导陈述诉求 → 逐项问清要素（关系状况、争议焦点、财产/标的、是否涉子女抚养、紧急程度、是否已委托律师、期望沟通时间）→ 中途当事人插话补充（演示打断）→ 当事人问一个专业问题（演示知识库命中）→ AI 确认信息、给出到所预约建议、礼貌结束 → 挂断后后台秒级弹出结构化成果。

---

## 4. 功能需求

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| **F1** | 演示留资页 + 发起通话 | P0 | H5 页面：填手机号、选案件类型，点"开始咨询"经 RTC 接通 Agent |
| **F2** | RTC 音频接入（MediaSession） | P0 | 浏览器采集麦克风、播放 AI 语音；通过 MediaSession 抽象层喂给 Agent |
| **F3** | 级联语音对话 Agent | P0 | 见下方 F3 细则 |
| **F4** | 实时字幕 / 转写展示 | P0 | 当事人端与后台同步显示双方对话文本，让观众"看见 AI 听懂了" |
| **F5** | 通话后处理 | P0 | 挂断后自动生成：客户档案 + 大/中/小分级 + 敏感/无效标记 + 预约信息 |
| **F6** | 演示后台看板 | P0 | 大屏展示：进行中通话+实时字幕；结束后展示结构化档案、分级、预约、录音回放 |
| **F7** | 敏感/无效咨询演示 | P1 | 可触发一通被标记过滤的咨询，展示风控 |
| **F8** | 录音存档 | P1 | 通话录音 + 转写留存，后台可回放（演示存证能力） |

### F3 级联语音对话 Agent（核心细则）

- **流式 ASR**：边说边转，部分结果即时上屏（中文电话/口语场景）。
- **文本 LLM**：
  - system prompt 注入**资深律师接待话术**（开场、共情、逐项追问、收尾预约的逻辑）；
  - **RAG** 挂单一细分领域知识库，支持当事人专业问题问答；
  - **Function Calling 槽位收集**：以工具调用方式逐步填满分诊要素（见 §8 字段），未问全不结束；
  - 结束时输出**结构化结果**（JSON）供通话后处理直接消费。
- **TTS**：自然语音播报；**可选拉伸项**——用律所主理律师的声音样本做**声音复刻**，作为演示彩蛋（需律师提供语音素材，有风险，非 P0）。
- **打断 / 端点检测**：支持 barge-in（当事人插话时 AI 立即停说并接住），由对话框架的 VAD + 端点检测实现。

---

## 5. 体验验收标准（Demo Acceptance）

| 指标 | 目标 | 可接受底线 |
|---|---|---|
| 端到端响应时延（用户说完 → AI 开口） | ≤ 1.2s | ≤ 1.8s |
| 打断生效（插话 → AI 停说） | ≤ 300ms | ≤ 500ms |
| 实时字幕延迟 | ≤ 500ms | ≤ 800ms |
| 演示问题集内知识库问答准确率 | ≥ 90% | ≥ 85% |
| 分诊必填要素收集完整率（演示场景） | 100% | 100% |
| 案件分级与律师人工判断一致（演示用例） | 一致 | 一致 |
| 单通演示对话稳定不掉线 | 100% | 100% |

> 验收以**预设演示用例集**为准（建议准备 3 通：1 通标准婚家、1 通含专业追问、1 通无效/敏感）。

---

## 6. 非功能需求

- **性能**：演示场景下并发 1–2 路即可，重在单路体验流畅。
- **稳定性**：演示前完成至少 2 轮全流程彩排；提供"重开一通"快速复位。
- **数据与录音**：演示数据可用测试数据；录音与转写仅用于演示存证展示，演示后可清理。
- **安全/隐私**：演示用手机号为演示者本人或虚拟号；不接入真实客户数据。
- **网络**：演示现场优先有线/稳定 Wi-Fi；准备 4G/5G 备份热点。

---

## 7. 技术方案概述（演示版）

```
当事人端 H5（RTC 客户端）
  · 留资页 → 开始咨询 · 麦克风采集 · AI 语音播放 · 实时字幕 · 挂断
            │  RTC（WebRTC）
            ▼
RTC 媒体服务（LiveKit / 火山引擎 RTC 二选一）
            │  音频流
            ▼
语音 Agent 服务  ──  MediaSession 抽象层（演示=RTC ingress；生产=SIP）
  级联：流式 ASR → 文本 LLM（话术 + RAG 知识库 + FC 槽位 + 结构化输出）→ TTS
  打断/VAD/端点：LiveKit Agents / Pipecat
            │  结构化结果 + 录音
            ▼
后端服务：Session 管理 · 知识库(向量) · 通话后处理(档案/分级/过滤/预约) · 录音存储
            │
            ▼
演示后台 H5（大屏看板）：进行中通话+字幕 / 结束后档案+分级+预约+录音回放
```

- **RTC 选型**：演示独立网页场景，**LiveKit** 起 cascade voice agent 最快（自带 Agents 框架与打断）；若希望与火山生态一致，可用**火山引擎 RTC**。二者均挂在 MediaSession 抽象层之下，对 Agent 透明。
- **复用承诺**：除 MediaSession 的 ingress 实现（RTC↔SIP）外，Agent、知识库、通话后处理、后台在生产期全部沿用。

---

## 8. 数据结构

### 8.1 分诊要素 / 客户档案字段

| 字段 | 必填 | 说明 |
|---|---|---|
| 当事人称呼 | 是 | 姓氏/称呼 |
| 联系电话 | 是 | 留资页填写 |
| 案件类型 | 是 | 一级 + 二级分类（限演示领域） |
| 所在地 | 是 | 省/市 |
| 争议金额 / 标的 | 是 | 用于分级 |
| 紧急程度 | 是 | 低/中/高 |
| 核心诉求 | 是 | AI 摘要 |
| 是否已有律师 | 是 | 是/否 |
| 期望沟通时间 | 是 | 用于预约 |
| 案件分级 | 自动 | 大/中/小 |
| 风险标记 | 自动 | 敏感词/无效/恶意/正常 |
| 录音 URL + 转写 | 自动 | 存证与回放 |

### 8.2 分级规则（演示版，简单可解释）

| 级别 | 触发条件（示例，阈值待律师确认） |
|---|---|
| 大 | 标的额 ≥ 阈值 **或** 高紧急 **或** 重大案件类型 |
| 中 | 标的额中等 **且** 要素完整 |
| 小 | 标的额低 / 咨询性质 / 信息不全 |

> v1 用"标的额 + 案件类型 + 紧急度"的简单规则或 prompt 实现即可，不上复杂模型；阈值与权重由律师团队给定基线。

---

## 9. 演示脚本 Runbook

1. **预备**：大屏打开演示后台（空看板）；当事人端备好手机/笔记本；网络与音频彩排过。
2. **留资**：当事人扫码进留资页 → 填手机号、选"婚姻家事" → 点"开始咨询"。
3. **接通**：RTC 接通，AI 用律师话术开场（自我介绍 + 说明先做初步登记）。
4. **对话**：演示者按预设案情口述；**故意中途打断一次**（展示 barge-in）；**故意问一个专业问题**（展示知识库命中）。
5. **收尾**：AI 复述确认信息 → 给出到所预约建议 → 礼貌结束通话。
6. **亮成果**：挂断后 3–5 秒，后台自动弹出该客户的**结构化档案 + 大/中/小分级 + 预约 + 录音回放 + 转写**。
7. **（可选）风控**：演示一通无效/敏感咨询，展示被标记过滤。

---

## 10. 里程碑与排期（演示版，约 2–3 周）

| 周 | 交付 |
|---|---|
| W1 | RTC 接入 + MediaSession + 级联 Agent 跑通（能对话、能打断）；单领域话术 v0 |
| W2 | 知识库接入 + 槽位收集 + 结构化产出；通话后处理（档案/分级/过滤）；演示后台看板 |
| W3 | 实时字幕、录音回放、演示脚本彩排与体验调优；（可选）声音复刻 |

---

## 11. 风险与依赖

- **体验风险**：级联时延/打断没调好会"卡"，直接砸演示——W3 必须留足彩排与调优时间，验收以 §5 指标为准。
- **内容风险**：知识库未覆盖的问题 AI 可能乱答——演示限定问题集，并在话术中设"超出范围转人工/到所"兜底话术。
- **依赖**：律师团队提供——演示细分领域、话术要点、分级阈值基线、（可选）声音素材。
- **范围蔓延**：演示中客户易临时加需求——以本 PRD 非目标清单为界，新增统一记入生产版 backlog。

---

## 12. 待确认项

1. 演示选哪个**单一细分领域**（建议婚姻家事）？
2. 分级**阈值与权重**基线（标的额档位、重大案件类型清单）？
3. RTC 选型：**LiveKit** vs **火山引擎 RTC**？
4. 是否做**声音复刻**彩蛋？若做，律师能否提供语音素材？
5. 演示形式：当事人端用**手机**还是**笔记本**？现场网络条件？
